#环境变量搭建
右击我的电脑——属性——高级——环境变量
修改path，编辑添加自己python的路径
成功添加环境变量后，可以用cmd直接运行python
--------------------------------------------
#用pip命令下载第三方库
打开命令行
输入cd：python的路径+\scripts   #这一步是打开pip.exe所在的文件夹
用了上一步命令后便可以直接pip install +下载库名
-------------------------------------------------
